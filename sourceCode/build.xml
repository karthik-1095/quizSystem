<project name="OnlineQuizSystem" default="deploy" basedir=".">

    <!-- Define properties -->
    <property name="Appmanage" value="C:\BW5\tra\5.7\bin\AppManage.exe" />
    <property name="build.dir" value="${basedir}/onlinequizsystem/Deployments" />
    <property name="app.name" value="OnlineQuizSystem" />
    <property name="project.bw.application" value="${basedir}/onlinequizsystem" />
    <!-- Define targets -->
	
   

    <target name="deploy" >
        <exec executable="${Appmanage}">
            <arg line="-deploy --propFile C:\BW5\tra\5.7\bin\AppManage.tra -app ${app.name} -ear ${build.dir}/EAR/onlinequizsystem.ear -deployconfig ${build.dir}/CONF/quizSystemConfig.xml -user admin -pw admin -domain admin" />
        </exec>
    </target>

</project>
